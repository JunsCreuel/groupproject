// 사격장 전체에서 공유하는 상수 — App.jsx(HUD 표시)와 Experience.jsx(발사 로직) 둘 다 사용
export const MAX_AMMO = 6;
export const RELOAD_MS = 900;
export const TOTAL_TARGETS = 12; // "TARGETS n/12" 표시 및 클리어 판정 기준
export const POINTS_PER_HIT = 10;
